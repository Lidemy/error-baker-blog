---
description: 依 AGENTS.md 的 Tag 正規化規範推薦 canonical tags（AI 推薦、人核可）
argument-hint: <posts/作者/檔名.md | backlog>
disable-model-invocation: true
---

請完整讀取並嚴格依照專案根目錄 `AGENTS.md` 的「Tag 正規化規範」處理：

    $ARGUMENTS

`AGENTS.md` 是 Tag 正規化流程的唯一真相來源：先執行其觸發驗證，再完成其中所有要求
（封閉詞彙表內推薦、每建議附依據、提案後待人工核可、寫入後必跑 `npm run check-tags`）。
本檔沒有任何額外規則。
