---
title: Archive
layout: layouts/home.njk
eleventyNavigation:
  key: Archive
  order: 1
pagination:
  # netlify.toml force-redirects /archive/:page to /archive/, so a second
  # pagination page would be unreachable — size must stay above the post count.
  data: collections.postsZhTW
  size: 9999
  alias: posts
  reverse: true
permalink: /archive/{% if pagination.pageNumber > 0 %}{{ pagination.pageNumber }}/{% endif %}
---

{# Archive is the full, single-page chronological index — better for browsing
   (Ctrl+F) and for SEO crawlability than page-by-page pagination. #}
{% set postslist = posts %}
{% include "postslist.njk" %}